spring.:
  application:
    name: weather-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/praddesh/weather-configurations
      discovery:
        enabled: true


logging:
  level:
    org:
      springframework: debug


server:
  port: 8888
